# 经方学习系统 v8 · 端口配置

> 文档编号：PORT-CONFIG-001  
> 版本：v1.0  
> 状态：已确认  
> 最后更新：2026-06-18  
> 维护者：经方学习系统交互设计系统

---

## 端口分配

本项目遵循全局端口治理方案，分配端口块 **8100-8109**。

| 端口 | 用途 | 状态 | 启动方式 |
|---|---|---|---|
| **8100** | 主端口（桌面版 `app/index.html`） | 活跃 | `python start_server.py` 或 `经方学习系统.bat` |
| **8101** | 备用端口（手机版 `app/mobile.html` / 冲突自动切换） | 活跃 | `python start_server.py 8101` 或 `经方学习系统-手机版.bat` |
| **8102** | 开发端口（v9 Vite dev server） | 预留 | 未来使用 |
| **8103-8109** | 扩展预留 | 未分配 | 待未来扩展 |

---

## 端口冲突处理

### 自动冲突检测

`start_server.py` 已内置端口冲突检测逻辑：

1. 启动时先检查主端口（8100）是否被占用
2. 如果被占用，自动尝试备用端口（8101）
3. 如果8101也被占用，报错退出并提示运行全局检测工具

### 手动冲突检测

```powershell
# 全局端口检测
python "C:\Users\Chen\Documents\Kimi\Workspaces\项目大管家\port_check.py"

# 快速检测单个端口
python -c "import socket; s=socket.socket(); print('占用' if s.connect_ex(('localhost',8100))==0 else '空闲'); s.close()"
```

---

## 禁止端口（全局冲突区）

以下端口为**禁止使用**端口，已被多个项目默认占用：

- 3000、5000、8000、8080、8081、9000

任何新启动脚本、新服务、新测试都**不得**使用上述端口。

---

## 历史变更

| 日期 | 变更 | 从 | 到 | 变更人 |
|---|---|---|---|---|
| 2026-06-18 | 端口迁移 | 8000 | 8100/8101 | 项目大管家（全局协调） |

---

## 相关文件

| 文件 | 说明 |
|---|---|
| `start_server.py` | 主启动脚本，内置冲突检测 |
| `经方学习系统.bat` | 桌面版一键启动，自动检测冲突 |
| `经方学习系统-手机版.bat` | 手机版一键启动，固定8101 |
| `AGENTS.md` | 项目规范，含端口管理章节 |
| `../port-registry.md` | 全局端口登记簿 |
| `../port_check.py` | 全局端口检测工具 |

---

*创建时间：2026-06-18*  
*项目根目录：`C:\Users\Chen\Desktop\经方学习系统（旧版）`*
