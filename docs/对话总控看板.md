---
date: 2026-06-22
version: 2.1
更新规则: 仅跨对话协作写入。用户手动更新。AI只读取，不写入。
---

# 对话总控看板

## AI触发器

> 你收到此文档 + 用户说"同步一下"或"看看任务"时，自动执行：
> 1. 扫描本文档中标题包含你角色关键词的section（如"方剂卡片""操作系统"等）
> 2. 提取该section的：当前状态、今日计划、阻塞/等待、给出去的、等进来的
> 3. 检查全局状态表和协作事项表，若涉及你，主动提醒
> 4. 输出：我是[角色]，状态[状态]，计划[计划]，阻塞[阻塞]，提醒[协作提醒]
> 5. 然后执行今日计划，或提醒用户阻塞需要协调

---

## 全局状态

| 当前重点 | 谁在阻塞 | 需用户决策 | 已闭环 |
|---------|---------|-----------|--------|
| 补太阴/厥阴卡片(P0) | 条文系统等方剂卡片发新卡 | 第4方审计选哪方？ | 3方审计闭环 |
| 启动第4方审计(P1) | 交互设计/审计智能体空闲 | mobile是否同步？ | 398条+63方+SP管线 |
| 临床录入优化(P1) | — | — | 搜索聚类+条文重构v2 |

---

## 协作事项（跨对话交接）

| 编号 | 任务 | 发送方 | 接收方 | 状态 | 需用户做什么 |
|------|------|--------|--------|------|-------------|
| X-001 | 太阴/厥阴6新卡+提示词 | 方剂卡片 | 条文系统 | 待生成 | 进方剂卡片对话，要求创建新卡 |
| X-002 | 第4方审计选方 | 用户 | 循证检索 | 等确认 | 从大承气/小柴胡/四逆/五苓中选一个 |
| X-003 | 新卡UI适配REQ-06 | 方剂卡片 | 操作系统 | 待生成 | 等方剂卡片完成新卡后生成需求 |
| X-004 | mobile同步决策 | 用户 | 操作系统 | 等决策 | 决定是否同步核心功能到移动端 |

---

## 对话路由速查

AI判断任务归属：
- 卡片/数据/JSON/Schema → 方剂卡片
- UI/按钮/弹窗/CSS/JS → 操作系统
- 条文/注家/讲解/原文 → 条文系统
- 解锁/交互/面板/加载 → 交互设计
- 审计/检索/文献/GRADE → 循证检索
- 验证/复核/检查报告 → 审计智能体

---

## 方剂卡片对话

**角色**：数据层。`data/*.json`生成验证、Schema设计、处方解析、药名别名。

**红线**：不写HTML/CSS/JS，不前端兼容，不循证检索，不条文讲解。

**状态**：🟢 活跃
**今日计划**：
- P0：补太阴2方（桂枝加芍药汤、桂枝加大黄汤）
- P0：补厥阴3方（乌梅丸、当归四逆汤、白头翁汤）
- P1：生成提示词→转交条文系统(X-001)
- P1：生成REQ-06→转交操作系统(X-003)
**阻塞**：无
**给出去**：待生成 X-001→条文系统，X-003→操作系统
**等进来**：无
**自检提醒**：新卡完成后必须发提示词给条文系统，否则对方一直等。

---

## 操作系统对话

**角色**：交互层。`app/index.html`的HTML/CSS/JS、点击/弹窗/跳转/状态/localStorage。

**红线**：不提取内容，不定义Schema，不生成JSON，不验证数据准确性，不循证检索。

**状态**：🟢 活跃
**今日计划**：
- P1：临床录入优化
- P1：响应式适配
- P2：mobile同步(等X-004用户决策)
- P2：v9重构检查
**阻塞**：X-004(mobile等用户决策)
**给出去**：无
**等进来**：X-003(REQ-06新卡UI适配，来自方剂卡片)
**自检提醒**：有待接收项X-003；单文件324KB臃肿，建议评估v9迁移。

---

## 条文系统对话

**角色**：内容层。`references.source_annotations`填充、条文讲解、质控。

**红线**：不修改卡片结构，不删其他卡references，不创建新卡，不写前端UI。

**状态**：🟡 等待
**今日计划**：
- P1：等接收新卡(X-001)后补充条文讲解
- P2：条文内容质控(P0-P3修复，7方待补)
**阻塞**：X-001(等方剂卡片发新卡)
**给出去**：已完成(扩展条文→交互设计)
**等进来**：X-001←方剂卡片
**自检提醒**：⚠️阻塞中。建议用户进方剂卡片对话要求创建新卡。等待期间可并行P0-P3修复。

---

## 交互设计系统对话

**角色**：UI/UX层。分级解锁、一方多条文展开、按需加载、交互组件。

**红线**：不改JSON结构，不改mastery，不删现有UI，不生成卡片内容，不循证检索。

**状态**：🔴 空闲
**今日计划**：待命，等数据需求
**阻塞**：等新卡扩展数据
**给出去**：无
**等进来**：扩展条文(已收)，新卡扩展数据(待收)
**自检提醒**：空闲。需新卡数据或扩展数据才激活。

---

## 循证文献检索专家

**角色**：审计层。笔记循证审计、文献检索、GRADE评级、修正JSON。

**红线**：不改HTML，不改formula_cards.json，不写localStorage，不生成报告以外文档，不自我验证。

**状态**：🟡 等待
**今日计划**：
- P1：启动第4方审计(候选:大承气/小柴胡/四逆/五苓)
- P2：扩展循证工作流
**阻塞**：X-002(等用户选方)
**给出去**：已完成(3方报告→审计智能体)
**等进来**：X-002←用户
**自检提醒**：⚠️等待中。需用户从候选方中选一个。当前3方已闭环，工作流已固化。

---

## 审计智能体

**角色**：质量复核。独立验证审计报告、三维验证(URL+内容+来源)。

**红线**：不生成新报告，不检索文献，不改报告以外文件，不生成修正JSON。

**状态**：🔴 空闲
**今日计划**：待命，等报告
**阻塞**：等循证检索提交新报告
**给出去**：验证结果→循证检索
**等进来**：新报告←循证检索
**自检提醒**：空闲。有新审计报告时激活验证。标准：PubMed+CNKI覆盖、A/B匹配、有效DOI、无遗漏。

---

## 更新日志

| 日期 | 内容 |
|------|------|
| 2026-06-22 | v2.1：极简触发器，去除人用说明，AI自驱动 |

---

*AI只读取本文档。用户手动更新。同步指令："同步一下"或"看看任务"。*
