# 当前任务对齐图

> 本页用流程图形式梳理：**我们现在在哪里、要往哪里去、下一步先做什么**。  
> 如果你发现图和实际想法不一致，请直接指出来，我们就在这里对齐。

---

## 图 1：现状 —— 我们手头有什么

```mermaid
flowchart TB
    subgraph RAW["原始资料层 raw/"]
        R1["annotations/<br/>倪海厦 txt / pdf"]
        R2["annotations-chm/<br/>10 个 CHM 源文件"]
        R3["extracted-chm/<br/>10 个已解压 CHM 目录"]
        R4["classical/<br/>伤寒论.doc / 桂林古本.doc"]
    end

    subgraph EXTRACTED["清洗产物层 extracted/"]
        E1["黄煌教授经方沙龙/<br/>✅ 已清洗 115 篇"]
        E2["annotations/<br/>倪海厦 extracted.md / cleaned.txt"]
        E3["其他：太阳病.md / 小红书笔记 / ..."]
    end

    subgraph DATA["卡片数据层 data/"]
        D1["formula_cards.json<br/>35 张方剂卡"]
        D2["source_cards.json<br/>35 张条文卡"]
        D3["experience_cards.json<br/>1 张经验卡"]
    end

    subgraph APP["前端应用层 app/"]
        A1["学习视图 / 练习 / 考试 / Kimi 导师"]
        A2["参考资料区（目前只有 10/35 有方链接）"]
        A3["经验卡区（目前只有 1 张，且有 bug）"]
    end

    R2 -->|解压| R3
    R3 -->|清洗| E1
    R3 -.->|未清洗| R3
    E1 -->|未接入| DATA
    E2 -->|未接入| DATA
    D1 --> A2
    D3 --> A3
```

**现状一句话**：原始资料很多，黄煌 CHM 已清洗但未接入系统，其他 CHM 还未清洗，卡片数据的经验层几乎空白。

---

## 图 2：目标 —— 我们要建立的数据飞轮

```mermaid
flowchart LR
    P1["原始资料<br/>CHM / TXT / PDF / DOC"]
    P2["清洗归档<br/>HTML → Markdown + catalog"]
    P3["主题提取<br/>按方剂/条文/医案分类"]
    P4["人工审阅<br/>确认主题相关 + 来源可靠"]
    P5["卡片入库<br/>formula / source / experience"]
    P6["前端使用<br/>学习 / 练习 / 考试"]
    P7["数据反馈<br/>错题 / 薄弱向量 / 覆盖度"]
    P8["补充资料<br/>缺什么补什么"]

    P1 --> P2 --> P3 --> P4 --> P5 --> P6 --> P7 --> P8 --> P1
```

**目标一句话**：让每一份原始资料都能经过“清洗 → 提取 → 审阅 → 入库 → 使用 → 反馈 → 再补充”的闭环，持续转动。

---

## 图 3：边界 —— 当前这轮不做的事

```mermaid
flowchart TB
    subgraph DO["✅ 当前做"]
        D1["黄煌资料接入学习系统<br/>（只做 L1 参考链接层）"]
        D2["确定参考资料选择原则"]
        D3["修复前端经验卡 bug"]
        D4["选一个试点方跑通端到端"]
    end

    subgraph LATER["⏸️ 后续做"]
        L1["81 篇医案结构化"]
        L2["27 篇药证补注"]
        L3["60/57/64/3 四个 CHM 清洗"]
        L4["大规模 experience_cards 生成"]
    end

    DO -.->|跑通后再做| LATER
```

**边界一句话**：当前不碰大规模清洗和医案结构化，先把“参考资料怎么挂、挂什么、挂完前端长什么样”跑通。

---

## 图 4：下一步路径 —— 我建议这样走

```mermaid
flowchart TD
    START["开始"]
    A["Step 1<br/>确定参考资料原则"]
    B["Step 2<br/>设计 references 字段"]
    C["Step 3<br/>选一个试点方<br/>建议：大柴胡汤"]
    D["Step 4<br/>从黄煌资料中找出<br/>与大柴胡汤相关的 3-5 篇"]
    E["Step 5<br/>生成 references<br/>挂到 formula_cards.json"]
    F["Step 6<br/>前端渲染验证"]
    G["Step 7<br/>你确认效果"]
    H["Step 8<br/>定型为模板<br/>推广到其他方"]

    START --> A --> B --> C --> D --> E --> F --> G --> H
```

---

## 我们现在要回答的三个关键问题

| 问题 | 为什么重要 | 当前状态 |
|---|---|---|
| **参考资料挂什么？** | 决定前端“参考资料区”显示什么内容 | 原则初稿已写，待你确认 |
| **参考资料怎么挂？** | 决定 `references` 字段结构和入库方式 | 需要设计 schema |
| **挂完怎么验证？** | 决定这套流程能不能推广 | 需要选一个试点方跑通 |

---

## 我用一句话总结

> **当前核心任务：把黄煌资料中“主题相关、来源可靠”的内容，以结构化的 references 形式挂到现有方剂卡上，前端能正确显示，并且这个过程能复制到其他方。**

如果这句话和你想的不一样，请直接改。
