# v8 经方学习系统 · 冒烟测试报告

> 测试时间：2026-06-14  
> 测试入口：`http://localhost:8000/app/index.html`  
> 测试方式：Kimi WebBridge + 本地 HTTP 服务  
> 当前卡片数：25 张 formula_card / 25 张 source_card

---

## 1. 环境检查

| 项目 | 状态 | 说明 |
|---|---|---|
| 本地 HTTP 服务 | ✅ | `python -m http.server 8000`，task `bash-blblwpna` |
| WebBridge 状态 | ✅ | `extension_connected: true`，daemon v1.9.17 |
| 页面加载 | ✅ | 标题正确，无 JS 报错，JSON 正常加载 |

---

## 2. 仪表盘

| 检查项 | 期望 | 结果 |
|---|---|---|
| 卡片总数 | 25 | ✅ 显示 25 张卡片 |
| 向量总数 | 150（25 × 6） | ✅ 显示「已掌握 0/150」 |
| 今日复习按钮 | 显示今日到期数 | ✅ 显示「今日复习 (143)」 |
| 主题切换 | 可切换深色/浅色 | ✅ 未报错 |

---

## 3. 学习视图

| 检查项 | 测试对象 | 结果 |
|---|---|---|
| 点击卡片进入学习页 | 大青龙汤 | ✅ 原文、症状谱、药物、煎服法、禁忌均显示 |
| 问 Kimi 弹窗 | 麻黄汤 | ✅ 弹窗打开，prompt 含方名、原文、必要症 |
| 返回卡片列表 | 任意卡片 | ✅ 可返回 |

---

## 4. 考试视图（单卡片）

| 检查项 | 测试对象 | 结果 |
|---|---|---|
| 开始测试 | 大青龙汤 | ✅ 生成 4 题 |
| 题型覆盖 | 0→1、1→0、0→2、0→contra | ✅ 均已出现 |
| 正确判定 | 大青龙汤必要症状 | ✅ 显示「回答正确」 |
| 错误判定 | 麻黄汤必要症状（故意选错） | ✅ 显示「回答错误」并给出正确答案 |
| 完成考试 | 任意卡片 | ✅ 返回学习页 |

---

## 5. 每日复习

| 检查项 | 期望 | 结果 |
|---|---|---|
| 生成题目 | 5 题 | ✅ 显示「题目 1 / 5」 |
| 题型覆盖 | 跨卡片随机/弱项优先 | ✅ 出现 0→usage（桂枝汤煎服法） |
| 判定 | 正确/错误 | ✅ 与单卡片考试一致 |

---

## 6. 持久化

| 检查项 | 操作 | 结果 |
|---|---|---|
| localStorage 写入 | 答题后 | ✅ `sh_index_v1_state` 中 `stats` 与 `cards.*.mastery` 更新 |
| 刷新不丢 | F5 刷新 | ✅ 已答题向量从今日复习中扣除（143 → 142） |
| 统计视图 | 点击「统计」 | ✅ 显示累计答题 8、正确 5、错误 3 |

---

## 7. 数据文件校验

```bash
python -c "import json; json.load(open('data/formula_cards.json', encoding='utf-8'))"
python -c "import json; json.load(open('data/source_cards.json', encoding='utf-8'))"
python -c "import json; json.load(open('data/experience_cards.json', encoding='utf-8'))"
```

全部通过，JSON 格式合法。

---

## 8. 发现的问题

| 问题 | 等级 | 说明 |
|---|---|---|
| `finishExam` 后直接返回学习页，无汇总弹窗 | 低 | 不影响学习闭环，可在 Phase 4 优化 |
| 煎服法选项文本过长，部分被截断 | 低 | UI 仍可点击，不影响判定 |
| 11 个 36 目标方尚未入库 | 中 | 不影响 25 张核心卡使用，后续继续补充 |

---

## 9. 结论

**冒烟测试通过。** 当前版本已满足周二前最小闭环：

- [x] 25 张太阳病类方卡片可学习
- [x] 4 种题型判定正确
- [x] 进度刷新不丢
- [x] 每日复习入口可用
- [x] Kimi 导师入口可用

下一步：继续补充剩余 11 张目标方，或进入 Phase 4（AI 导师一键 Kimi、工程文档）。
